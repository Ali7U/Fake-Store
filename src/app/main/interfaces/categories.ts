export interface Categories {
  id: number | null;
  name: string;
  slug?: string;
  image?: string;
  creationAt?: string;
  updatedAt?: string;
}

export interface Category{
    name: string,
    image: string
}